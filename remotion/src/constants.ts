export const FPS = 30;
export const WIDTH = 1080;
export const HEIGHT = 1920;

export const msToFrames = (ms: number) => Math.round((ms / 1000) * FPS);

export const SERIES_ROOT = "../series/learn-ai-in-2-mins";
